{
    "release-it": {
        "npm": {
            "publish": true,
            "registry": "https://registry.npmjs.org/"
        },
        "git": {
            "requireCleanWorkingDir": false,
            "tagName": "v${version}",
            "release": true,
            "token": "{{secrets.GH_TOKEN}}"
        }
    }
}
