{
    "release-it": {
        "npm": {
            "publish": true,
            "registry": "https://registry.npmjs.org/"
        },
        "github": {
            "release": true,
            "releaseName": "Release ${version}",
            "token": "{{secrets.GITHUB_TOKEN}}",
            "assets": ["dist/*"]
        }
    }
}
