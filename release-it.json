{
    "release-it": {
        "npm": {
            "publish": true,
            "registry": "https://registry.npmjs.org/"
        },
        "github": {
            "release": true,
            "releaseName": "Release ${version}",
            "token": "{{secrets.GH_TOKEN}}",
            "assets": ["dist/*"]
        }
    }
}
